# After: V8 垃圾回收机制深度解析（Borderline 案例）

> 这是一个"大部分正确"的案例。检测发现 0 项明确错位 + 2 项 Borderline。
> 修正仅针对 Borderline 项做可选调整，其余全部保留。

## 文章结构（修正后）

```
V8 垃圾回收机制深度解析

  一、V8 的内存堆结构                                      ← 10%，不变
  二、Scavenge 算法：新生代回收                             ← 15% → 10%，压缩实现细节
  三、写屏障（Write Barrier）                               ← 10% → 5%，压缩为一段话
  四、标记-清除与标记-压缩：三色标记法                       ← 15%，不变
  五、增量标记与并发标记                                     ← 10%，不变
  六、隐藏类与内联缓存                                       ← 20%，不变
  总结                                                      ← 5%，不变
```

## 修正操作清单

| 操作 | 依据 | 必要性 |
|------|------|--------|
| 压缩 Scavenge 实现细节 | Transport：Cheney 指针细节对理解 GC 行为帮助有限 | 可选——"深度解析"允许保留 |
| 压缩写屏障 Card Marking | Transport：512 字节卡片大小是实现细节 | 可选——同上 |
| 其余内容不动 | 全部 ✅ Correct | — |

## 二次检测

```
- [✅ Correct] 内存堆结构：不变
- [✅ Correct] Scavenge：压缩后权重降低，Transport 更合理
- [✅ Correct] 写屏障：压缩后权重降低，Transport 更合理
- [✅ Correct] 三色标记法：不变
- [✅ Correct] 增量/并发标记：不变
- [✅ Correct] 隐藏类/IC：不变（主题外溢问题仍在，但不在本 skill 范围内）
```

## 本案例展示的要点

1. **"大部分正确"不需要大改**：修正仅涉及 2 个可选的压缩操作，文章结构不变。
2. **Borderline 的处置**：skill 标记为 ⚠️ 而非 ❌，用户自行决定是否调整。
3. **skill 的边界**：隐藏类/IC 权重正确但主题外溢——本 skill 不管这个，诚实标注。
